node_modules
dist
.env
.env.local
.env.*.local
.vercel
.DS_Store
*.log
brand/logo-animated.mp4
.env*

# Промежуточные артефакты генератора отчёта аудита: сам PDF версионируется,
# черновые прогоны и картинки для визуальной проверки — нет.
audit/.build/*.pdf
audit/.build/chk/
audit/.build/pk/
audit/.build/ql/
