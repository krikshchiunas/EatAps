import { writeFileSync } from 'node:fs'
import { Doc, C, SEV_COLOR } from './layout.mjs'
import { FINDINGS, COUNTS, COMMIT, COMMIT_SHORT, BRANCH, DATE } from './findings.mjs'
import { buildRows } from './coverage.mjs'
import { part2 } from './sections2.mjs'
import { part3 } from './sections3.mjs'

const ROWS = buildRows()
const TOC_PAGES = 2

const SCORES = [
  ['Функциональная корректность', 8, 'Ядро работает, 813 из 814 тестов зелёные. Снимают балл незакрытая верхняя граница количества и NaN при повреждённом профиле.'],
  ['Безопасность', 6, 'RLS сплошной и грамотный, подмены личности нет нигде. Снимают баллы гонка бюджета AI и отказ в высший тариф.'],
  ['Приватность', 4, 'Модель доступа в базе образцовая, но изображения не удаляются никогда, а правовые документы не знают о трёх обработчиках.'],
  ['Целостность базы', 8, 'RLS на всех 33 таблицах, каскады полные, save_app_state атомарен. Снимает балл разъехавшийся setup_all.sql.'],
  ['Офлайн и синхронизация', 9, 'HLC, тумбстоуны, пофайловые метки, кэш по пользователю, 814 тестов вокруг слияния. Сильнейшая часть проекта.'],
  ['Платежи', 6, 'Подпись вебхука проверяется, клиент тариф не назначает. Снимают баллы отказ в AI_PREMIUM и отсутствие контроля порядка событий.'],
  ['Безопасность AI', 6, 'Границы в промпте продуманы, тариф серверный. Снимают баллы обход лимита и отсутствие второго рубежа.'],
  ['Производительность', 7, 'Ни одной утечки эффектов на 80 проверенных. Снимает балл нераздёленный чанк на 1,15 МБ.'],
  ['Пользовательский опыт', 8, 'Отмена, подтверждения, честные пустые состояния, все ветки отказа сканера. Снимает балл рассинхронизация тумблеров приватности.'],
  ['Доступность', 6, '126 aria-label и корректные роли, но десять кнопок-символов без имени на главном экране.'],
  ['Покрытие тестами', 8, '814 тестов, включая многоустройственные сценарии и сторожей SQL. Не покрыты RLS, платежи и лимиты AI.'],
  ['Сопровождаемость', 8, 'Комментарии объясняют причины, а не механику. Снимают балл мёртвый Web3 и exchange.js.'],
  ['Наблюдаемость', 4, 'Есть структурный log и console.error в критичных местах, но ни сбора ошибок, ни метрик, ни оповещений.'],
  ['Готовность к продакшену', 5, 'Инженерно приложение зрелое; к выпуску не готовы правовые документы и три пункта P1.'],
]

const TOP10 = [
  ['SEC-001', 'P1', 'Обход дневного лимита AI параллельными запросами', 'Деньги владельца, обесценивание подписки', 'Высокая — скрипт в 10 строк', 'S (<1 ч)'],
  ['PRIV-001', 'P1', 'Anthropic, Stripe и Telegram не раскрыты; политика — шаблон', 'Нарушение ст. 13/28/30 GDPR, блокирует запуск в ЕС', 'н/п — состояние документов', 'M (часы)'],
  ['PRIV-002', 'P1', 'Изображения не удаляются даже при удалении аккаунта', 'Нарушение ст. 17, бессрочная публичная ссылка', 'Средняя — нужна сохранённая ссылка', 'M → L'],
  ['SEC-002', 'P2', 'Незаданная цена даёт высший тариф AI_PREMIUM', 'Бесплатный безлимит на дорогой модели', 'Низкая — нужна редкая форма события', 'S (<1 ч)'],
  ['SEC-003', 'P2', '/api/feedback открыт без входа', 'Затопление личного Telegram владельца', 'Высокая — один curl', 'S (<1 ч)'],
  ['DB-001', 'P2', 'setup_all.sql разошёлся с миграциями', 'Расхождение схемы при установке с нуля', 'н/п — состояние репозитория', 'S (<1 ч)'],
  ['SEC-004', 'P2', 'Вебхук Stripe без контроля порядка событий', 'Платный доступ после отмены', 'Низкая — зависит от доставки', 'M (часы)'],
  ['PRIV-003', 'P2', 'Выгрузка не включает переписку и социальный граф', 'Неполное исполнение ст. 15/20', 'н/п', 'M (часы)'],
  ['SEC-005', 'P3', 'Блокировка не действует в общих группах', 'Канал для преследования в обход блокировки', 'Низкая — нужен посредник', 'S (<1 ч)'],
  ['UX-001', 'P3', 'Количество продукта не ограничено сверху', 'Порча статистики обычной опечаткой', 'Высокая — случайно', 'S (<1 ч)'],
]

const QUICKWINS = [
  ['SEC-001', 'Вернуть итог из ai_usage_add и сверять с бюджетом', 'S'],
  ['SEC-002', 'Строить TIER_BY_PRICE только из заданных переменных', 'S'],
  ['SEC-003', 'Потребовать вход в /api/feedback (как в /api/support)', 'S'],
  ['DB-001', 'Внести миграцию в SOURCES и пересобрать setup_all.sql', 'S'],
  ['CFG-001', 'Взять ADMIN_CHAT_IDS из окружения в api/feedback.js', 'S'],
  ['UX-001', 'Верхняя граница в sanitizeAmount — одно место на 6 полей', 'S'],
  ['BUG-001', 'Проверка входа в computeTargets, возврат null', 'S'],
  ['A11Y-001', 'aria-label на десять кнопок-символов', 'S'],
  ['SEC-005', 'Убрать «recipient is null or» из политики чтения messages', 'S'],
  ['SEC-007', 'Сверка сигнатуры файла в /api/ai/vision', 'S'],
  ['UX-002', 'После ошибки в PrivacyHub перечитывать состояние с сервера', 'S'],
  ['DB-002', 'Дописать set search_path в 16 функций', 'S'],
  ['SEC-006', 'Убрать unsafe-eval, сузить connect-src', 'S'],
  ['PERF-001', 'manualChunks для foods.js и supabase', 'S'],
  ['DEP-001', 'Удалить выключенный стек Web3 — минус 12 предупреждений', 'M'],
  ['PRIV-001', 'Дополнить Datenschutz, VVT и чек-лист AVV', 'M'],
  ['SEC-004', 'last_event_at и отбрасывание старых событий Stripe', 'M'],
  ['CODE-001', 'Подключить exchange.js к выгрузке либо удалить', 'M'],
  ['PRIV-003', 'Серверная сборка полной выгрузки одной RPC', 'M'],
  ['PRIV-002', 'Удаление объектов хранилища + закрытие бакетов', 'L'],
]

const INVARIANTS = [
  ['И-01', 'Дневник виден только тем, кого впустил владелец', 'can_view_diary + политика app_state', '✅ держится'],
  ['И-02', 'Заблокированный не может написать в личные сообщения', 'can_message + is_blocked_between', '✅ держится'],
  ['И-03', 'Заблокированный не виден заблокировавшему нигде', 'политика messages select', '🚨 нарушается в группах (SEC-005)'],
  ['И-04', 'Реакция принадлежит вошедшему, а не user_id из запроса', 'toggle_post_reaction: auth.uid()', '✅ держится'],
  ['И-05', 'Сообщение нельзя отправить от чужого имени', 'политика insert: auth.uid() = sender', '✅ держится'],
  ['И-06', 'Получатель может менять только read_at', 'guard_message_update', '✅ держится'],
  ['И-07', 'Уведомление нельзя подделать', 'нет политики INSERT; push_notification revoked от authenticated', '✅ держится'],
  ['И-08', 'Промокод нельзя погасить сверх max_uses', 'for update + CHECK used <= max_uses', '✅ держится'],
  ['И-09', 'Один промокод — одно гашение на человека', 'первичный ключ (user_id, code)', '✅ держится'],
  ['И-10', 'Клиент не может сам повысить тариф', 'tierOf читает базу; вебхук проверяет подпись', '✅ держится'],
  ['И-11', 'Тариф не выдаётся при неизвестной цене', 'TIER_BY_PRICE[priceId] || FREE', '🚨 нарушается (SEC-002)'],
  ['И-12', 'FREE не тратит больше дневного бюджета', 'checkBudget + reserve', '🚨 нарушается при параллелизме (SEC-001)'],
  ['И-13', 'Состояние не перезаписывается устаревшей версией', 'save_app_state: where revision = base', '✅ держится'],
  ['И-14', 'Удалённая запись не воскресает при слиянии', 'тумбстоуны в syncModel', '✅ держится'],
  ['И-15', 'Данные ушедшего не остаются на устройстве', 'clearCache по владельцу при выходе', '✅ держится (кроме недосланного)'],
  ['И-16', 'Удаление аккаунта стирает все его данные', 'каскады от auth.users', '🚨 нарушается для хранилища (PRIV-002)'],
  ['И-17', 'Закрытый аккаунт не отдаёт содержимое посторонним', 'can_view_profile_content', '✅ держится'],
  ['И-18', 'Приватный ответ не попадает в кэш service worker', 'sw.js: чужой origin и /api/ не трогаются', '✅ держится'],
]

function render(tocData) {
  const d = new Doc()

  // ── Обложка
  d.suppressChrome = true
  d.newPage()
  const P = d.pdf, pg = d.page
  P.rect(pg, 0, P.H - 180, P.W, 180, [0.11, 0.12, 0.14])
  P.rect(pg, 52, P.H - 92, 46, 4, C.accent)
  P.text(pg, { x: 52, y: P.H - 132, size: 30, font: 'bold', str: 'EatAps', color: [1, 1, 1] })
  P.text(pg, { x: 52, y: P.H - 158, size: 13, font: 'reg', str: 'Полный аудит приложения', color: [0.72, 0.74, 0.78] })

  let y = P.H - 240
  const line = (k, v, bold) => {
    P.text(pg, { x: 52, y, size: 9, font: 'reg', str: k, color: C.ink3 })
    P.text(pg, { x: 190, y, size: 9.5, font: bold ? 'bold' : 'reg', str: v, color: C.ink })
    y -= 19
  }
  line('Дата аудита', DATE)
  line('Репозиторий', '/Users/denyskrikshchiunas/Projects/EatAps')
  line('Ветка', BRANCH)
  line('Аудируемый коммит', COMMIT, true)
  line('Версия приложения', '0.1.0')
  line('Объём кода', '~75 800 строк · 73 компонента · 64 модуля · 12 535 строк SQL')

  y -= 14
  P.line(pg, 52, y, P.W - 52, y, C.rule, 0.7)
  y -= 30
  P.text(pg, { x: 52, y, size: 11, font: 'bold', str: 'Итог', color: C.ink })
  y -= 26

  // Плитки severity
  const tiles = [['P0', COUNTS.P0], ['P1', COUNTS.P1], ['P2', COUNTS.P2], ['P3', COUNTS.P3], ['P4', COUNTS.P4]]
  let tx = 52
  const tw = (P.W - 104 - 4 * 10) / 5
  for (const [s, n] of tiles) {
    P.rect(pg, tx, y - 44, tw, 58, C.band)
    P.rect(pg, tx, y + 11, tw, 3, SEV_COLOR[s])
    const ns = String(n)
    P.text(pg, { x: tx + (tw - d.f.bold.widthOf(ns, 22)) / 2, y: y - 14, size: 22, font: 'bold', str: ns, color: n ? SEV_COLOR[s] : C.ink3 })
    P.text(pg, { x: tx + (tw - d.f.reg.widthOf(s, 8)) / 2, y: y - 34, size: 8, font: 'reg', str: s, color: C.ink2 })
    tx += tw + 10
  }
  y -= 70

  const total = FINDINGS.length
  const cov = ((ROWS.filter((r) => r.status !== '⚠️ PARTIALLY AUDITED' && r.status !== '❌ NOT TESTABLE').length / ROWS.length) * 100)
  const score = SCORES.reduce((s, x) => s + x[1], 0)
  const pct = Math.round((score / (SCORES.length * 10)) * 100)

  const stat = (k, v, col) => {
    P.text(pg, { x: 52, y, size: 9, font: 'reg', str: k, color: C.ink3 })
    P.text(pg, { x: 190, y, size: 10.5, font: 'bold', str: v, color: col || C.ink })
    y -= 21
  }
  stat('Всего находок', String(total))
  stat('Готовность к продакшену', `${pct} / 100`, C.p1)
  stat('Покрытие аудитом', `${cov.toFixed(1)} %  (${ROWS.length} элементов)`)
  stat('Сборка', 'npm run build — успешно')
  stat('Тесты', '813 из 814 пройдено, 1 провален (sqlGuard)', C.p2)

  y -= 10
  P.rect(pg, 52, y - 34, P.W - 104, 42, [0.99, 0.94, 0.90])
  P.rect(pg, 52, y - 34, 3.5, 42, C.p1)
  P.text(pg, { x: 64, y: y - 4, size: 12, font: 'bold', str: '🟠  ТРЕБУЮТСЯ СУЩЕСТВЕННЫЕ ИСПРАВЛЕНИЯ', color: C.ink })
  P.text(pg, { x: 64, y: y - 22, size: 8.8, font: 'reg', str: 'Инженерно приложение зрелое. К выпуску не готовы правовые документы и три пункта P1.', color: C.ink2 })

  P.text(pg, { x: 52, y: 70, size: 7.6, font: 'reg', str: 'Аудит выполнен статическим разбором, запуском сборки и тестов, а также прогоном расчётных модулей на граничных', color: C.ink3 })
  P.text(pg, { x: 52, y: 58, size: 7.6, font: 'reg', str: 'входных данных. Браузерный и боевой прогон не проводился — см. раздел «Границы аудита».', color: C.ink3 })
  d.suppressChrome = false

  // ── Оглавление: место резервируется, чтобы номера страниц не поехали
  const tocPageIdx = []
  for (let i = 0; i < TOC_PAGES; i++) { d.newPage(); tocPageIdx.push(d.pageNo) }
  if (tocData) {
    let pi = 0, yy = 0
    const start = (i) => {
      d.page = d.pdf.pages[tocPageIdx[i] - 1]
      yy = d.pdf.H - d.M.t
      if (i === 0) {
        d.pdf.rect(d.page, d.M.l, yy - 4, 34, 3, C.accent)
        yy -= 26
        d.pdf.text(d.page, { x: d.M.l, y: yy, size: 20, font: 'bold', str: 'Содержание', color: C.ink })
        yy -= 30
      }
    }
    start(0)
    for (const e of tocData) {
      if (yy < d.M.b + 16) { pi++; if (pi >= TOC_PAGES) break; start(pi) }
      const size = e.level === 1 ? 9.6 : 8.8
      const ind = e.level === 1 ? 0 : 16
      const num = String(e.page)
      const nw = d.f.reg.widthOf(num, size)
      const label = e.str
      const maxW = d.CW - ind - nw - 20
      let t = label
      while (d.f[e.level === 1 ? 'bold' : 'reg'].widthOf(t, size) > maxW && t.length > 4) t = t.slice(0, -2)
      if (t !== label) t += '…'
      if (e.level === 1) yy -= 5
      d.pdf.text(d.page, { x: d.M.l + ind, y: yy, size, font: e.level === 1 ? 'bold' : 'reg', str: t, color: e.level === 1 ? C.ink : C.ink2 })
      const tw2 = d.f[e.level === 1 ? 'bold' : 'reg'].widthOf(t, size)
      const dots = d.M.l + ind + tw2 + 4
      d.pdf.line(d.page, dots, yy + 2.5, d.M.l + d.CW - nw - 5, yy + 2.5, [0.88, 0.89, 0.91], 0.4)
      d.pdf.text(d.page, { x: d.M.l + d.CW - nw, y: yy, size, font: 'reg', str: num, color: C.ink3 })
      yy -= e.level === 1 ? 15.5 : 13.2
    }
  }

  // ═══ 1. Резюме
  d.h1('1. Резюме для владельца')
  d.p('EatAps — трекер питания в виде PWA: дневник еды с расчётом КБЖУ и микронутриентов, синхронизация между устройствами с работой офлайн, социальная часть (подписки, закрытые аккаунты, лента, личные и групповые сообщения), AI-ассистент на моделях Anthropic, подписки через Stripe и модерация через Telegram-бота.')
  d.p('Первое, что нужно сказать прямо: это не типовой проект, собранный из шаблонов. Инженерная культура здесь заметно выше средней. Ряд вещей, которые в аудитах обычно составляют список претензий, здесь сделаны правильно ещё до аудита.')

  d.h2('1.1. Что сделано хорошо')
  for (const s of [
    'Row Level Security включён на ВСЕХ 33 таблицах без исключений, а таблица промокодов намеренно оставлена вовсе без политик — то есть закрыта по умолчанию. Это встречается редко.',
    'Все функции с правами определившего (SECURITY DEFINER) объявлены с set search_path = public. Проверка сплошная, по всем 148 функциям.',
    'Подмены личности нет нигде: реакции, сообщения, подписки и настройки везде опираются на auth.uid(), а не на идентификатор из запроса. Отдельно проверялись самые частые места таких ошибок — ни одного совпадения.',
    'Уведомления невозможно подделать: политики INSERT у таблицы нет вовсе, а функция push_notification отозвана у роли authenticated.',
    'Гашение промокодов защищено правильно и на двух уровнях сразу: SELECT … FOR UPDATE в функции и ограничение CHECK (used <= max_uses) в самой таблице. Гонка «сто человек на последнее место» здесь невозможна.',
    'Синхронизация между устройствами — сильнейшая часть проекта: гибридные логические часы, тумбстоуны на каждое удаление, отдельные метки времени на каждое поле дня, кэш с ключом по пользователю (перепутать аккаунты физически нечем) и атомарный compare-and-swap по revision в save_app_state.',
    'Service worker не кэширует ничего приватного: чужие origin (то есть весь Supabase) и /api/ пропускаются мимо кэша, ссылки входа тоже. Приватные данные следующему пользователю устройства не достанутся.',
    'Из 80 проверенных useEffect, ставящих слушатели, таймеры или подписки, ровно ноль оставлены без функции очистки.',
    'XSS-поверхности нет: ни dangerouslySetInnerHTML, ни innerHTML, ни eval во всём проекте; автолинковка в чате разбирает только http(s).',
    'Ограничения частоты на изменяющие действия реализованы в базе триггерами, а не в интерфейсе: подписки, публикации, ответы, реакции, сообщения незнакомым.',
    'Сканер штрихкода обрабатывает все шесть ветвей отказа (нет разрешения, нет камеры, камера занята, небезопасный контекст, не поддерживается, прочий сбой), и в каждой даёт выход «Добавить вручную».',
    'Собственная оснастка вокруг SQL: сборка setup_all.sql из источников, проверка идемпотентности, аудит политик, каталог схемы — и всё это под автотестами.',
    '814 автотестов, включая сценарии на два устройства, слияние состояний и защиту от воскрешения удалённых записей.',
    'Комментарии в коде объясняют ПРИЧИНУ решения и то, что сломалось раньше, а не пересказывают код. Это заметно упрощает сопровождение.',
  ]) d.bullet(s)

  d.h2('1.2. Что мешает выпуску')
  d.p('Найдено 22 подтверждённых расхождения. Критических (P0) нет: ни захвата аккаунта, ни массовой утечки приватных данных, ни обхода оплаты в полном смысле обнаружить не удалось. Но три пункта уровня P1 выпускать в таком виде нельзя.')
  d.note('Главная проблема проекта — не в коде, а в документах. Правовые тексты описывают приложение, каким оно было до появления AI-ассистента, подписок и поддержки, и показываются людям с красной плашкой «Шаблон» и незаполненными полями вида [Name], [Anschrift]. Для приложения с немецкими юридическими текстами, обрабатывающего данные о здоровье, это блокирующий пункт.', { title: 'Если читать только одну строку' })
  for (const f of FINDINGS.filter((x) => x.sev === 'P1')) {
    d.h3(`${f.id} — ${f.title}`)
    d.p(f.impact, { size: 9.2, color: C.ink2 })
  }

  d.h2('1.3. Оценка')
  d.table(
    [{ t: 'Направление', w: 3 }, { t: 'Балл', w: 0.7 }, { t: 'Чем обоснован', w: 6.6 }],
    SCORES.map(([k, v, why]) => [{ t: k, bold: true }, { t: `${v}/10`, align: 'r', color: v >= 8 ? C.ok : v >= 6 ? C.p2 : C.p1 }, why])
  )
  d.p(`ИТОГОВАЯ ОЦЕНКА: ${score} из ${SCORES.length * 10} → ${pct} / 100`, { font: 'bold', size: 11 })
  d.note('🟠 ТРЕБУЮТСЯ СУЩЕСТВЕННЫЕ ИСПРАВЛЕНИЯ — до устранения трёх пунктов P1 приложение не следует открывать для широкой публики в ЕС. Для закрытого теста на ограниченном круге согласившихся пользователей состояние приемлемо.', { color: C.p1, title: 'Вердикт' })

  // ═══ 2. Границы аудита
  d.h1('2. Границы аудита — что проверено и что нет')
  d.p('Отчёт разделяет проверенное от предполагаемого. Ниже честно перечислено, чего этот аудит НЕ делал, чтобы ни одна оценка не была принята за большее, чем она есть.')
  d.h2('2.1. Что выполнено фактически')
  for (const s of [
    'Сплошное чтение кода: 73 компонента, 64 модуля, 8 серверных точек, 12 535 строк SQL, 24 миграции, service worker, конфигурация сборки и развёртывания.',
    'Запуск сборки: npm run build — успешно, 11,12 с, предупреждения о размере чанков зафиксированы.',
    'Запуск тестов: npm test — 813 из 814 пройдено, один провал (sqlGuard) разобран и вошёл в находку DB-001.',
    'Запуск собственной оснастки проекта: policy-audit, check-sql, sql-idempotency, sql-catalog, build-setup-all --check.',
    'Прогон расчётных модулей на граничных данных: computeTargets, scale, sanitizeAmount, amountLabel, formatAmount — 15 наборов входных данных, включая ноль, отрицательные, 1e12, NaN, Infinity, запятую вместо точки, нечисловые строки и нелатинские цифры.',
    'Проверка семантики TIER_BY_PRICE запуском на реальных значениях окружения — находка SEC-002 подтверждена исполнением, а не рассуждением.',
    'npm audit с разбором каждой из 17 записей по дереву зависимостей.',
    'Автоматический разбор всех 80 useEffect на наличие функции очистки.',
    'Автоматический разбор всех определений функций SQL на security definer и search_path.',
  ]) d.bullet(s)
  d.h2('2.2. Чего аудит НЕ делал')
  d.note('Ни один запрос к боевым адресам (eataps.com, Supabase, Stripe, Telegram, Anthropic) не отправлялся. Данные реальных пользователей не затрагивались. Все сценарии воспроизведения в отчёте описаны, но НЕ исполнены против прода.', { color: C.p3, title: 'Важно' })
  for (const s of [
    'Браузерный прогон интерфейса не проводился: в окружении нет ни Chromium, ни средств автоматизации браузера. Все выводы по экранам и кнопкам получены чтением кода. Поэтому по каждому экрану в матрице покрытия стоит «runtime: нет».',
    'Живой экземпляр Postgres недоступен, поэтому политики RLS проверены разбором предикатов, а не выполнением запросов от имени разных пользователей. Это существенная оговорка: разбор ловит логические дыры, но не ловит ошибок исполнения.',
    'Боевое окружение Vercel недоступно: какие переменные там заданы — неизвестно. Выводы о STRIPE_PRICE_AI_PREMIUM и VITE_WALLETCONNECT_PROJECT_ID сделаны по отсутствию документации в .env.example и помечены соответствующим уровнем уверенности.',
    'Живых прогонов AI-модели не выполнялось: устойчивость к внедрению инструкций и качество медицинских границ оценены по тексту промпта, а не по поведению.',
    'Вход через Google (OAuth) и через кошелёк Web3 не проверялись сквозным прогоном: первый требует боевых ключей, второй выключен.',
    'Нагрузочное тестирование, проверка на реальных мобильных устройствах, аудит Lighthouse и проверка контраста пикселями не проводились.',
  ]) d.bullet(s)
  d.h2('2.3. Как читать степень уверенности')
  d.table(
    [{ t: 'Метка', w: 1.4 }, { t: 'Что означает', w: 8 }],
    [
      [{ t: 'CONFIRMED', bold: true }, 'Дефект доказан: исполнением кода, выводом инструмента или однозначным чтением. Спорить не о чем — вопрос только в приоритете.'],
      [{ t: 'PROBABLE', bold: true }, 'Путь рассуждения проверен по коду, но сам сценарий не воспроизводился. Требует подтверждения перед тем, как считать фактом.'],
      [{ t: 'HIGH / MEDIUM', bold: true }, 'Уверенность в оценке ПОСЛЕДСТВИЙ и достижимости, отдельно от факта существования дефекта.'],
    ], { zebra: false }
  )

  // ═══ 3. Архитектура
  d.h1('3. Архитектура')
  d.h2('3.1. Общее устройство')
  d.p('Приложение построено по принципу «сначала локально»: состояние живёт на устройстве, облако — способ синхронизации, а не источник истины. Отсюда вытекает почти вся его архитектура.')
  d.code(`Браузер / PWA
  ├── React 18 + Vite 6  (без маршрутизатора: экраны — состояние, а не адреса)
  ├── store.jsx          единое состояние + 40 мутаций + гибридные логические часы
  ├── localStorage       кэш с ключом eataps:state:<uid> — по пользователю
  ├── service worker     только статические ресурсы; /api/ и чужие origin не кэширует
  └── syncEngine         очередь отправки, повтор, слияние, статусы
        │
        ├── Supabase  ── Postgres (33 таблицы, RLS на всех)
        │                148 функций, 110 доступны роли authenticated
        │                Realtime: состояние, сообщения, события, подписки
        │                Storage: post-images, chat-images (публичные),
        │                         dm-media (закрытый, подписанные ссылки)
        │
        └── /api/*  ── Vercel Functions (8 точек)
              ├── ai/chat, ai/vision      → Anthropic (ключ только на сервере)
              ├── stripe/checkout, portal, webhook
              ├── telegram/webhook        → модерация
              └── support, feedback       → Telegram владельца`)

  d.h2('3.2. Три решения, на которых держится проект')
  d.h3('Состояние как один документ')
  d.p('Весь дневник пользователя — одна строка app_state с полем jsonb и счётчиком revision. Запись идёт через save_app_state с условием where revision = p_base_revision: это compare-and-swap, атомарный по построению. Проигравший гонку получает out_conflict = true и текущее состояние сервера, после чего сливает его у себя. Решение нестандартное, но для дневника оправданное и реализовано корректно.')
  d.h3('Права считаются по подпискам, а не по отдельной таблице дружбы')
  d.p('Друзья — это взаимная подписка. Таблица friendships осталась только якорем для уведомления «теперь вы друзья», а все права считает is_friend_with по follows. Это закрывает целый класс расхождений, когда право переписки и право видеть дневник разъезжались. В проекте есть даже отдельный тест-сторож, следящий, чтобы политика дневника спрашивала is_friend_with, а не читала строку friendships.')
  d.h3('Приватность многослойная, и слои перекрывают друг друга сверху вниз')
  d.p('Блокировка сильнее всего, затем закрытость аккаунта, затем выбранный владельцем круг, и отдельно — поимённые разрешения (доступ к дневнику, тренер), которые закрытость не отменяет. Предикаты can_view_diary, can_view_post, can_view_profile_content написаны последовательно и согласованно; расхождений между ними при проверке не обнаружено.')

  d.h2('3.3. Значения по умолчанию')
  d.p('Отдельно отмечу выбор значений по умолчанию — он консервативный, то есть в пользу приватности. Это редкость и её стоит сохранить.')
  d.table(
    [{ t: 'Настройка', w: 2.6 }, { t: 'По умолчанию', w: 1.6 }, { t: 'Оценка', w: 5.8 }],
    [
      ['Аккаунт закрыт (is_private)', 'false', 'Открытый по умолчанию — обычная практика для социальной части, приемлемо.'],
      ['Видимость дневника', 'mutuals', { t: '✔ Верно: самое личное открыто только взаимным подпискам, а не всем.', color: C.ok }],
      ['Сообщения от подписок', 'direct', 'Разумно: на кого подписан — тому доверяешь.'],
      ['Сообщения от подписчиков', 'request', { t: '✔ Верно: незнакомец попадает в запросы, а не в чаты.', color: C.ok }],
      ['Сообщения от остальных', 'request', { t: '✔ Верно.', color: C.ok }],
      ['Приглашения в группы', 'following', { t: '✔ Верно — и именно это ограничивает последствия SEC-005.', color: C.ok }],
      ['Отчёты о прочтении', 'true', 'Спорно, но симметрично и отключаемо.'],
    ]
  )

  // ═══ 4. Инвентарь
  d.h1('4. Инвентарь приложения')
  d.h2('4.1. Числа')
  d.table(
    [{ t: 'Что', w: 4 }, { t: 'Сколько', w: 1.2 }, { t: 'Примечание', w: 5 }],
    [
      ['Файлов под контролем версий', '282', 'без node_modules и dist'],
      ['Строк кода (всего)', '~75 800', 'src + api + supabase + scripts'],
      ['React-компонентов', '73', 'включая messaging/ и social/'],
      ['Экранов (монтируются целиком)', '35', 'перечислены в разделе 6'],
      ['Модулей src/lib', '64', 'без тестовых файлов'],
      ['Тестовых файлов', '49', '814 тестов'],
      ['Интерактивных обработчиков', '756', '538 onClick, 187 onChange, 8 onSubmit, 23 прочих'],
      ['Тегов <button>', '405', ''],
      ['Полей ввода', '105', 'input, textarea, select'],
      ['Серверных точек /api', '8', ''],
      ['Таблиц Postgres', '33', 'RLS включён на всех 33'],
      ['Функций Postgres', '148', '110 доступны роли authenticated'],
      ['RPC, вызываемых клиентом', '68', 'все имеют соответствующий grant'],
      ['Политик RLS (последние редакции)', '99', '113 операторов create policy с учётом истории'],
      ['Триггеров', '33', ''],
      ['Индексов', '47', ''],
      ['Миграций', '24', 'одна не внесена в сборку — DB-001'],
    ]
  )
  d.h2('4.2. Распределение интерактивных элементов')
  d.p('Сложность сосредоточена в листе добавления еды: 134 обработчика, 81 кнопка и 46 полей ввода в одном файле на 2 267 строк — это самый нагруженный экран приложения и главный кандидат на разделение (см. раздел 15).')
  d.table(
    [{ t: 'Компонент', w: 4 }, { t: 'Обработчиков', w: 1.4 }, { t: 'Кнопок', w: 1 }, { t: 'Полей', w: 1 }],
    [['AddMealSheet.jsx', '134', '81', '46'], ['DayScreen.jsx', '48', '32', '2'], ['SettingsPanels.jsx', '40', '10', '3'],
     ['SupplementSheet.jsx', '29', '19', '6'], ['ThoughtsFeed.jsx', '29', '16', '3'], ['Onboarding.jsx', '25', '7', '4'],
     ['ConversationInfo.jsx', '19', '7', '1'], ['ChallengesScreen.jsx', '19', '10', '4'], ['ChatScreen.jsx', '18', '7', '0'],
     ['PrivacyHub.jsx', '17', '1', '0'], ['остальные 63 компонента', '378', '215', '36']].map((r) => [r[0], { t: r[1], align: 'r' }, { t: r[2], align: 'r' }, { t: r[3], align: 'r' }])
  )

  // ═══ 5. Сборка и тесты
  d.h1('5. Сборка, тесты и оснастка')
  d.h2('5.1. Результаты запусков')
  d.table(
    [{ t: 'Команда', w: 2.4 }, { t: 'Итог', w: 1.2 }, { t: 'Что зафиксировано', w: 6.4 }],
    [
      ['npm test', { t: 'ЧАСТИЧНО', color: C.p2, bold: true }, '813 из 814 пройдено за 1,8 с. Провал: «setup_all.sql собран из источников и не разошёлся с ними» (src/lib/sqlGuard.test.js:37) → находка DB-001.'],
      ['npm run build', { t: 'УСПЕХ', color: C.ok, bold: true }, 'Собрано за 11,12 с. Предупреждение rollup о чанках больше 500 КБ → PERF-001. Скрипт inject-precache отработал.'],
      ['npm run sql:verify', { t: 'ПРОВАЛ', color: C.p1, bold: true }, 'Падает на первом шаге — тот же DB-001.'],
      ['node scripts/check-sql.mjs', { t: 'УСПЕХ', color: C.ok, bold: true }, 'Все SQL-файлы, включая новую миграцию, прошли статическую проверку.'],
      ['node scripts/sql-idempotency.mjs', { t: 'УСПЕХ', color: C.ok, bold: true }, 'Нарушений нет на 23 файлах — повторный прогон миграций безопасен.'],
      ['node scripts/policy-audit.mjs', { t: 'УСПЕХ', color: C.ok, bold: true }, 'Выведены все политики по таблицам; использован как основа раздела 9.'],
      ['npm audit', { t: 'ЗАМЕЧАНИЯ', color: C.p2, bold: true }, '17 уязвимостей (10 умеренных, 7 высоких) → DEP-001.'],
      ['npm outdated', { t: 'ЗАМЕЧАНИЯ', color: C.p2, bold: true }, 'React 18 при доступном 19, Vite 6 при 8, supabase-js и stripe на минор отстают.'],
    ]
  )
  d.h2('5.2. Оснастка проекта — сильная сторона')
  d.p('Проект содержит собственные инструменты контроля SQL, которых обычно нет даже в зрелых кодовых базах: детерминированная сборка файла полной установки, сплошная проверка идемпотентности каждого создаваемого объекта, аудит политик, сверка каталога схемы с миграциями. Всё это вызывается из тестов, то есть нарушение ловится автоматически.')
  d.p('Единственная претензия к этой оснастке — не к ней самой, а к тому, что её сигнал сейчас проигнорирован: тест DB-001 честно упал, но код с расхождением остался в рабочем дереве.')
  d.h2('5.3. Пробелы в покрытии тестами')
  d.table(
    [{ t: 'Область', w: 2.6 }, { t: 'Покрытие', w: 1.5 }, { t: 'Что нужно добавить', w: 5.9 }],
    [
      ['Слияние и синхронизация', { t: 'отличное', color: C.ok }, 'Сценарии на два устройства, тумбстоуны, конфликты — покрыто лучше всего в проекте.'],
      ['Расчёты питания', { t: 'хорошее', color: C.ok }, 'Добавить граничные входы: пустой профиль, Infinity, отрицательные (см. BUG-001).'],
      ['Сторожа SQL', { t: 'отличное', color: C.ok }, 'Дополнить проверкой search_path у всех функций (DB-002).'],
      ['Политики RLS', { t: 'нет', color: C.p1 }, 'Главный пробел. Нужны интеграционные тесты на живой базе: два пользователя, блокировка, закрытый аккаунт, круги дневника.'],
      ['Лимиты AI', { t: 'частичное', color: C.p2 }, 'aiBudget покрыт как чистая функция; конкурентный доступ не проверяется вовсе — именно там SEC-001.'],
      ['Платежи', { t: 'минимальное', color: C.p2 }, 'Покрыт только safeOrigin. Нужны тесты на отображение цены в тариф и на порядок событий.'],
      ['Аутентификация', { t: 'частичное', color: C.p2 }, 'authState и authErrors покрыты; сквозных сценариев входа и выхода нет.'],
      ['Приватность (сквозная)', { t: 'нет', color: C.p1 }, 'Нет теста, проверяющего, что скрытое в интерфейсе действительно недоступно через API.'],
    ]
  )

  part2(d)
  part3(d, { INVARIANTS, TOP10, QUICKWINS, ROWS, SCORES })
  return d
}

export function buildDoc() {
  const p1 = render(null)
  return render(p1.toc)
}
