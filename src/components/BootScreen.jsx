// Единственный экран загрузки приложения.
//
// Он существует именно для того, чтобы НИЧЕГО другого в этот момент не
// показывалось: пока неизвестно, есть ли сессия и чьи данные лежат локально,
// любой экран будет неверным. Раньше за эти доли секунды успевали мелькнуть
// онбординг, кнопка «Войти» и главный экран с нулями.
export default function BootScreen({ slow = false }) {
  return (
    <div className="bootscreen">
      <img src="/logo.png" width="96" height="96" alt="" />
      <div className="boot-spinner" aria-hidden="true" />
      <p className="boot-hint" role="status">
        {slow ? 'Медленное соединение — восстанавливаем данные…' : 'Загружаем ваши данные…'}
      </p>
    </div>
  )
}
